apiVersion: v1
kind: Service
metadata:
  name: erpulse-api
  labels:
    app: erpulse-api
spec:
  type: LoadBalancer
  selector:
    app: erpulse-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP